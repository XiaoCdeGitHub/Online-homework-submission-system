学生端接口
1. 上传作业
接口描述： 学生提交作业文件
请求URL： /user/uploadHomework
请求方式： POST (multipart/form-data)
请求参数：
| 参数名 | 类型 | 必选 | 描述 |
|-------|------|------|------|
| file | File | 是 | 作业文件，支持格式：zip、rar、doc、docx、md、jpg、png |
| userId | String | 是 | 用户ID |
| comments | String | 否 | 提交说明/评论 |
| weeks | Integer | 是 | 作业周数 |
| submitTime | String | 是 | 提交时间，格式：yyyy-MM-dd HH:mm:ss |
响应结果：
}
2. 获取个人历史作业
接口描述： 查询学生个人的历史作业提交记录
请求URL： /user/historySubmit
请求方式： POST
请求体格式：
}
响应结果：
}
3. 获取该方向的本周任务
接口描述： 获取指定方向和周数的作业任务
请求URL： /user/recentTask
请求方式： POST
请求体格式：
}
响应结果：
}
4. 获取该方向的全部任务
接口描述： 获取指定方向的所有历史作业任务
请求URL： /user/allTask
请求方式： POST
请求体格式：
}
响应结果：
}
5. 获取提交统计信息
接口描述： 获取用户的作业提交统计信息
请求URL： /user/getCount
请求方式： POST
请求体格式：
}
响应结果：
}
管理员端接口
1. 发布作业
接口描述： 管理员发布新作业
请求URL： /admin/publishHomework
请求方式： POST (multipart/form-data)
请求参数：
| 参数名 | 类型 | 必选 | 描述 |
|-------|------|------|------|
| file | File | 是 | 作业文件，支持格式：zip、rar、doc、docx、md、jpg、png |
| notice | String | 是 | 作业说明 |
| startTime | String | 是 | 开始时间，格式：yyyy-MM-dd HH:mm:ss |
| endTime | String | 是 | 截止时间，格式：yyyy-MM-dd HH:mm:ss |
| weeks | Integer | 是 | 周数 |
| direction | String | 是 | 方向（如：前端、后端、产品等） |
响应结果：
}
2. 下载学生作业
接口描述： 管理员下载学生提交的作业文件
请求URL： /admin/downloadHomework
请求方式： GET
请求参数：
| 参数名 | 类型 | 必选 | 描述 |
|-------|------|------|------|
| userId | String | 是 | 学生用户ID |
| weeks | Integer | 是 | 作业周数 |
响应结果：
成功：直接下载文件（二进制流）
文件响应头：Content-Disposition: attachment; filename=xxx
3. 条件查询作业
接口描述： 按条件查询作业提交情况
请求URL： /admin/selectCondition
请求方式： POST
请求体格式：
}
响应结果：
}
4. 导出作业报告
接口描述： 导出特定条件下的学生作业情况Excel报告
请求URL： /admin/getFinalExcel
请求方式： GET
请求参数：
| 参数名 | 类型 | 必选 | 描述 |
|-------|------|------|------|
| direction | String | 是 | 方向 |
| group | String | 是 | 小组 |
| weeks | Integer | 是 | 周数 |
响应结果：
成功：直接下载Excel文件（二进制流）
文件名格式：{direction}{group}-作业情况统计.xlsx
5. 获取小组作业提交统计
接口描述： 获取特定小组的作业提交统计数据
请求URL： /admin/getGroupInfo
请求方式： POST
请求体格式：
}
响应结果：
}
6. 获取当前周数
接口描述： 获取系统当前的最新周数
请求URL： /admin/getCurrentWeeks
请求方式： GET
响应结果：
直接返回整数值
文件规格说明
支持的文件类型
.zip - 压缩文件
.rar - 压缩文件
.doc - Word文档
.docx - Word文档
.md - Markdown文档
.jpg - 图片
.png - 图片
文件大小限制
请参考后端 application.yml 中的配置：
100MB
错误码说明
200: 请求成功
500: 请求失败
注意事项
所有文件上传请求需使用 multipart/form-data 格式
上传接口需要处理文件为空的情况
文件下载接口将返回二进制流，前端需要正确处理下载响应
用户接口和管理员接口分开，请注意权限控制